venv/
__pycache__/
*.pyc
.env
*.jpg
*.jpeg
*.png
test_images/

# Catalog images must ship with the deployed app, so un-ignore them
# even though *.jpg/*.jpeg/*.png are blanket-ignored above.
!catalog_images/
!catalog_images/**

# README screenshots, same reason - they're the first thing anyone sees
# on the repo, so they have to be committed rather than ignored.
!docs/
!docs/**